# 🔮 EdTech Attendance Analytics & Forecasting Dashboard

> Интерактивный аналитический дашборд **Power BI**, интегрированный с локальной базой данных **MongoDB**, реализующий логику автоматической очистки временных рядов от аномалий и сквозное машинное обучение (**ML**) для прогнозирования посещаемости на 6 месяцев вперед.

---

## 🎯 Обзор проекта
Этот проект трансформирует сырые выгрузки из базы данных образовательного центра в стратегический аналитический инструмент. Он успешно решает три ключевые задачи:

1. **🗄️ Data Engineering (ETL в СУБД):** Реляционное объединение коллекций (`lessons`, `attendances`, `groups`) на уровне базы данных MongoDB с помощью агрегированных представлений (Views) и жесткая фильтрация целевого онлайн-филиала.
2. **📈 Time-Series Pipeline:** Автоматизированный скрипт очистки данных, который отсекает неполные граничные периоды, устраняет локальные просадки с помощью скользящего среднего (пороговый фильтр 1.5 стандартных отклонений) и применяет медианный fallback-алгоритм.
3. **🤖 Predictive Modeling:** Модель временных рядов на базе линейной регрессии (`scikit-learn`), которая учитывает глобальный тренд развития школы и компоненты календарной сезонности, выстраивая непрерывный прогноз на 6 месяцев вперед.

---

## 🛠️ Стек технологий
- **База данных:** MongoDB 🍃 (Агрегированные представления / Views, Mongosh)
- **ETL / Трансформация:** Power Query ⚡ + Python 🐍 (Pandas, NumPy)
- **Машинное обучение:** Scikit-Learn 🧠 (LinearRegression)
- **BI / Визуализация:** Power BI Desktop 📊 (Меры DAX, Аналитические трендлайны, Карточки KPI, SVG-иконки для современного минималистичного интерфейса)

---

## 📁 Структура репозитория
```text
├── 📂 data/                    # Исходные таблицы + результат пайплайна
│   ├── 📄 attendances.csv      # Выгрузка посещаемости студентов
│   ├── 📄 groups.csv           # Справочник учебных групп и филиалов
│   ├── 📄 lessons.csv          # Расписание проведенных занятий
│   └── 📄 predictions_output.csv # Итоговый файл с прогнозом модели
├── 📂 reports/                 # Слой бизнес-аналитики и отчетности
│   └── 📊 dashboard.pbix       # Финальный интерактивный дашборд Power BI
├── 📂 screenshots/             # Визуализации и скриншоты дашборда для документации
│   └── 🖼️ 1.png                # Главный скриншот обзорного дашборда
├── 📂 scripts/                 # Скрипты автоматизации СУБД и ML-пайплайна
│   ├── 📜 create_view.js       # Сценарий сборки представления для Mongosh
│   ├── ⚙️ db_import.py         # Скрипт первичного импорта CSV -> MongoDB
│   └── ⚙️ pipeline.py          # Автономный скрипт трансформации и предсказания
├── 🛠️ .gitignore               # Исключение системного кэша Python и логов BI
├── 📝 README.md                # Главная документация проекта
└── 📋 requirements.txt         # Окружение зависимостей Python
```

---

## 🚀 Инструкция по локальному развертыванию

### 1. Подготовка репозитория и окружения
Клонируйте репозиторий в локальную папку и установите необходимые библиотеки Python (убедитесь, что на вашем компьютере установлен Python и запущен локальный сервер MongoDB Community Server):

```bash
git clone https://github.com
cd EdTech-analitics-ml
pip install -r requirements.txt
```

### 2. Развертывание базы данных MongoDB
Для автоматического создания структуры базы данных и наполнения её коллекциями выполните скрипт первичного импорта:

```bash
python scripts/db_import.py
```

После этого откройте MongoDB Compass, перейдите в созданную базу данных `EdTechDB`, откройте встроенную консоль **Mongosh** внизу экрана и выполните JS-сценарий для сборки агрегированного представления:

```bash
mongosh mongodb://localhost:27017/EdTechDB scripts/create_view.js
```
*Результат:* В вашей СУБД появится готовое отфильтрованное представление `bd_online_aggregated`.

### 3. Фоновый запуск ML-пайплайна
Вы можете запустить автономный скрипт прогнозирования для расчета метрик (среднее, медиана) и выгрузки результатов предсказания в плоский файл без открытия интерфейса BI:

```bash
python scripts/pipeline.py
```
Файл с предсказаниями на 6 месяцев автоматически сохранится по пути `data/predictions_output.csv`.

### 4. Запуск аналитического дашборда
1. Установите и откройте **Power BI Desktop**.
2. Откройте файл отчета `reports/dashboard.pbix`.
3. Нажмите кнопку **Обновить (Refresh)** на главной панели инструментов. 

Power BI автоматически подключится к вашей локальной MongoDB через встроенный Python-коннектор, пересчитает модель линейной регрессии и обновит все интерактивные графики, KPI-карточки («Всего посещений», «Медиана за историю», «Среднее за историю») и линии тренда на холсте.